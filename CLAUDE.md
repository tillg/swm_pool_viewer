# Claude Code Guidelines

## Commits

- Always commit specs (in `Specs/` folder) together with the related code changes in a single commit

## Playwright Screenshots

- Save all Playwright screenshots to the `.tmp/` directory (e.g., `.tmp/screenshot.png`)
